# On Windows the cached Spring test contexts keep airsonic.log and cache/.lock open,
# so JUnit's @TempDir cleanup fails with "Failed to close extension context" and marks
# otherwise-passing test classes as errored. Leave temp dirs for the OS to clean up.
junit.jupiter.tempdir.cleanup.mode.default = never
